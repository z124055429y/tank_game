#include <gtest/gtest.h>

#include "scene/game_scene.hpp"

TEST(SceneTest, ToNextStage) {
    // GameScene scene;
    // scene.init();
    // scene.refresh();
    // scene.input('d');
    // scene.refresh();
    // for (size_t i = 0; i < 200; i++)
    // {
    //     scene.input('c');
    //     scene.refresh();
    // }
    
}